// ==UserScript==
// @name          Bypass.VIP Premium Bypasser (API Key Required)
// @namespace     bypass.vip
// @version       3.3.1
// @author        rootacce3ss
// @description   Inspired by ZXRK's bypass.vip userscript. This one supports a premium API key, has all the bugs fixed and will have more up to date links. Be sure to configure below, where the API key is requested.
// @match         *://loot-link.com/*
// @match         *://best-links.org/*
// @match         *://loot-links.com/*
// @match         *://megaspremium.com/*
// @match         *://lootdest.com/*
// @match         *://direct-links.net/*
// @match         *://risquemega.com/*
// @match         *://onlyfriends.club/*
// @match         *://onepiecered.co/*
// @match         *://multileaks.com/*
// @match         *://luvsquad-links.com/*
// @match         *://lootdest.org/*
// @match         *://free-leaks.com/*
// @match         *://goldmega.online/*
// @match         *://mega-guy.com/*
// @match         *://realiukzemydre.com/*
// @match         *://kmendation.com/*
// @match         *://lootlinks.co/*
// @match         *://onlyshare.info/*
// @match         *://leakplugs.com/*
// @match         *://nswfbox.com/*
// @match         *://eofmukindwo.com/*
// @match         *://op-packs.com/*
// @match         *://thedetective.online/*
// @match         *://forlinkmeg.com/*
// @match         *://thegoatpack.org/*
// @match         *://of-leaks.xyz/*
// @match         *://links.spacebin.in/*
// @match         *://supermeg.com/*
// @match         *://onlyfanessereloaded.com/*
// @match         *://fanzleaks.com/*
// @match         *://cemendemons.com/*
// @match         *://premiummegaz.com/*
// @match         *://thepremium.online/*
// @match         *://pasteebins.com/*
// @match         *://baddiezcentral.com/*
// @match         *://megazone.website/*
// @match         *://leak-pragmatic.com/*
// @match         *://megadumpz.com/*
// @match         *://thhaven.net/*
// @match         *://meg4fans.com/*
// @match         *://depravityweb.co/*
// @match         *://discordlink.cc/*
// @match         *://megashub.co/*
// @match         *://streamergirls.org/*
// @match         *://your-leaks.com/*
// @match         *://onlylinksmegas.xyz/*
// @match         *://pypy.spacebin.in/*
// @match         *://pypy.in/*
// @match         *://luvsquad-links.cmo/*
// @match         *://beast.net.in/*
// @match         *://hook.beast.net.in/*
// @match         *://all-fans.online/*
// @match         *://fansmega.com/*
// @match         *://worldpacks.co/*
// @match         *://night-hub.online/*
// @match         *://dailyofleaks.com/*
// @match         *://pkofs.com/*
// @match         *://offree90.com/*
// @match         *://megadropz.com/*
// @match         *://onlyfunlink.com/*
// @match         *://direct-links.org/*
// @match         *://leaks4you.com/*
// @match         *://onlylinksmegas.com/*
// @match         *://direct-link.net/*
// @match         *://downbadleaks.com/*
// @match         *://onlyfanscloud.com/*
// @match         *://missleakz.com/*
// @match         *://leakszone.online/*
// @match         *://links-loot.com/*
// @match         *://holyfanslinks.com/*
// @match         *://utopianleaks.com/*
// @match         *://megavip.store/*
// @match         *://drlinker.com/*
// @match         *://baddiesheaven.com/*
// @match         *://of4lm-links.com/*
// @match         *://holedonly.store/*
// @match         *://lootlinks.com/*
// @match         *://free-content.pro/*
// @match         *://milky-center.com/*
// @match         *://megaofs.com/*
// @match         *://bleleadersto.com/*
// @match         *://link-target.org/*
// @match         *://daughablelea.com/*
// @match         *://mymegalinks.com/*
// @match         *://heroslut.com/*
// @match         *://tonordersitye.com/*
// @match         *://vip-linknetwork.com/*
// @match         *://birdbiss.com/*
// @match         *://loot-labs.com/*
// @match         *://lootlabs.com/*
// @match         *://link-hub.net/*
// @match         *://locconnect.com/*
// @match         *://premiumstashdrop.com/*
// @match         *://lootdest.info/*
// @match         *://of-area.com/*
// @match         *://link-target.net/*
// @match         *://megalnk.com/*
// @match         *://lootlink.org/*
// @match         *://nsfwcherry.com/*
// @match         *://cherrypacks.online/*
// @match         *://hotstars-leaks.com/*
// @match         *://onlymega.co/*
// @match         *://hanimeturks.com/*
// @match         *://dailyleakz.com/*
// @match         *://content-hub.club/*
// @match         *://dailyadultmegas.com/*
// @match         *://oui-chu.com/*
// @match         *://megalinks.one/*
// @match         *://starleakz.com/*
// @match         *://babeslink.click/*
// @match         *://ofgirls3x.com/*
// @match         *://pnp-drops.me/*
// @match         *://nsfw-paradise.club/*
// @match         *://key-access.co/*
// @match         *://leaksmix.com/*
// @match         *://sensual-leaks.com/*
// @match         *://ofhub-leaks.com/*
// @match         *://onlyforfan.online/*
// @match         *://sweetjuice-mega.com/*
// @match         *://crip-hub.com/*
// @match         *://leaksfreeday.com/*
// @match         *://thotshaven.online/*
// @match         *://mega-leaked.com/*
// @match         *://beautifulgirls.social/*
// @match         *://tavernleaks.com/*
// @match         *://nudeleaksteenz.com/*
// @match         *://slutywet.com/*
// @match         *://leakutopia.site/*
// @match         *://secret-packs.com/*
// @match         *://attiktok22.com/*
// @match         *://pancakes-leaks.com/*
// @match         *://diamond-leaks.com/*
// @match         *://lewd-leaks.com/*
// @match         *://ftbleaks.net/*
// @match         *://thepytheaven.org/*
// @match         *://of-region.online/*
// @match         *://admiregirls-byme.com/*
// @match         *://mzehoney12.com/*
// @match         *://herplace.online/*
// @match         *://megaplugleaks.com/*
// @match         *://of-kingdom.com/*
// @match         *://onlyfansmegafolder.com/*
// @match         *://links.spacebin.in/*
// @match         *://adfoc.us/*
// @match         *://boost.ink/*
// @match         *://cuty.io/*
// @match         *://www.cuty.io/*
// @match         *://cety.app/*
// @match         *://www.cety.app/*
// @match         *://linkvertise.com/*/*
// @match         *://mboost.me/*
// @match         *://bst.gg/*
// @match         *://booo.st/*
// @match         *://mendationforc.info/*
// @match         *://paster.so/*
// @match         *://paster.gg/*
// @match         *://rekonise.com/*
// @match         *://social-unlock.com/*
// @match         *://www.social-unlock.com/*
// @match         *://socialwolvez.com/*
// @match         *://sub2get.com/*
// @match         *://www.sub2get.com/*
// @match         *://sub2unlock.com/*
// @match         *://sub2unlock.io/*
// @match         *://sub2unlock.net/*
// @match         *://sub2unlock.online/*
// @match         *://sub2unlock.top/*
// @match         *://sub4unlock.pro/*
// @match         *://sub4unlock.com/*
// @match         *://sub4unlock.io/*
// @match         *://subfinal.com/*
// @match         *://unlocknow.net/*
// @match         *://v.gd/*
// @match         *://work.ink/*
// @match         *://www.work.ink/*
// @match         *://workink.net/*
// @match         *://r.work.ink/*
// @match         *://workink.one/*
// @match         *://workink.me/*
// @match         *://ytsubme.com/*
// @match         *://www.ytsubme.com/*
// @match         *://esohasl.net/*
// @match         *://rbscripts.net/*
// @match         *://link.rbscripts.net/*
// @match         *://luarmor.net/*
// @match         *://ads.luarmor.net/*
// @match         *://*.luarmor.net/*
// @match         *://loot-labs.gg/*
// @match         *://lootlabs.gg/*
// @match         *://admaven.com/*
// @match         *://justpaste.it/*
// @match         *://paste-drop.com/*
// @match         *://pastebin.com/*
// @match         *://pastecanyon.com/*
// @match         *://pastehill.com/*
// @match         *://pastemode.com/*
// @match         *://rentry.org/*
// @match         *://bit.ly/*
// @match         *://cl.gy/*
// @match         *://goo.gl/*
// @match         *://is.gd/*
// @match         *://rebrand.ly/*
// @match         *://rkns.link/*
// @match         *://shorter.me/*
// @match         *://t.co/*
// @match         *://t.ly/*
// @match         *://tiny.cc/*
// @match         *://tinylink.onl/*
// @match         *://tinyurl.com/*
// @grant         GM_xmlhttpRequest
// @connect       api.bypass.vip
// @run-at        document-start
// @downloadURL   https://github.com/rootacc3ss/bypass-userscripts/raw/refs/heads/main/bypass.vip/premium-bypass-vip.user.js
// @updateURL     https://github.com/rootacc3ss/bypass-userscripts/raw/refs/heads/main/bypass.vip/premium-bypass-vip.user.js
// ==/UserScript==

(async () => {
    if (window.top !== window.self) {return;};
    'use strict';
    
    // ========================================
    // CONFIGURATION SETTINGS - EDIT THESE!
    // ========================================
    // YOU MUST GET AN API KEY FROM THE BYPASS.VIP DISCORD!
    const config = {
        apiKey: 'x-api-key', // Your API key here - REQUIRED!
        autoRedirect2s: false, // Set to true for auto redirect after 2 seconds
        autoRedirect5s: false, // Set to true for auto redirect after 5 seconds (only one can be true)
        newTab: true // Open bypass links in new tab
    };
    
    // Luarmor configuration - define first before detection
    const luarmorConfig = {
        time: 10,
        key: '',
        safeMode: true
    };

    // Luarmor functions - exact replica of original script
    function createLuarmorContainer() {
        const container = document.createElement('div');
        container.id = 'userscript-container';
        container.style.cssText = `
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-color: #121212;
            color: #e0e0e0;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            text-align: center;
            padding: 40px;
            z-index: 2147483647;
            font-family: 'Arial', sans-serif;
            overflow: auto;
            box-shadow: inset 0 0 20px rgba(0, 0, 0, 0.5);
            pointer-events: auto;
        `;
        container.innerHTML = `
            <h2 style="font-size: 2em; margin-bottom: 15px; color: #ffffff; text-shadow: 0 2px 4px rgba(0, 0, 0, 0.3);">BYPASS.VIP USERSCRIPT</h2>
            <p style="margin-bottom: 30px; font-size: 1.1em; color: #b0b0b0;">Click the button below to proceed to the bypassed link.</p>
            <div id="countdown" style="font-size: 1.3em; margin-bottom: 30px; padding: 10px; background: #1e1e1e; border-radius: 8px; width: 80%; max-width: 600px;"></div>
            <button id="nextBtn" type="button" style="
                padding: 12px 24px;
                background-color: #1E88E5;
                color: #ffffff;
                border: none;
                border-radius: 8px;
                cursor: pointer;
                transition: background-color 0.3s, transform 0.2s;
                font-size: 1.1em;
                box-shadow: 0 4px 8px rgba(0, 0, 0, 0.4);
                position: relative;
                z-index: 2147483647;
                pointer-events: auto;
            ">Next</button>
            <div id="errorMsg" style="color: #ff4d4d; margin-top: 30px; display: none; font-size: 1.1em; background: #2a2a2a; padding: 10px; border-radius: 8px;"></div>
            <div id="spinner" style="border: 5px solid #333333; border-top: 5px solid #1E88E5; border-radius: 50%; width: 30px; height: 30px; animation: spin 1s linear infinite; display: none; margin-top: 20px;"></div>
            <style>
                @keyframes spin { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }
                #nextBtn:hover { background-color: #1565C0; transform: translateY(-2px); }
                #nextBtn:active { transform: translateY(0); }
                #nextBtn:disabled {
                    pointer-events: none;
                    opacity: 0.6;
                    cursor: not-allowed;
                }
            </style>
        `;
        return container;
    }

    function showLuarmorError(message) {
        const errorEl = document.getElementById('errorMsg');
        if (errorEl) {
            errorEl.textContent = message;
            errorEl.style.display = 'block';
        }
        console.error(message);
    }

    function isValidUrl(url) {
        try {
            new URL(url);
            return true;
        } catch {
            return false;
        }
    }

    async function handleLuarmorApiBypass() {
        console.log('Handling LuArmor API bypass for:', window.location.href);
        
        // Show loading container while calling API
        const container = createLuarmorContainer();
        if (document.body) {
            document.body.appendChild(container);
        } else {
            document.documentElement.appendChild(container);
        }
        
        const statusDiv = container.querySelector('#countdown');
        const btn = container.querySelector('#nextBtn');
        const spinner = container.querySelector('#spinner');
        
        statusDiv.textContent = 'Bypassing LuArmor link...';
        btn.disabled = true;
        spinner.style.display = 'block';
        
        try {
            // Call the bypass API
            const result = await new Promise((resolve, reject) => {
                const requestUrl = `https://api.bypass.vip/premium/bypass?url=${encodeURIComponent(window.location.href)}`;
                const requestHeaders = {
                    'x-api-key': config.apiKey,
                    'Content-Type': 'application/json'
                };
                
                GM_xmlhttpRequest({
                    method: 'GET',
                    url: requestUrl,
                    headers: requestHeaders,
                    onload: (response) => {
                        try {
                            if (response.status === 200) {
                                const data = JSON.parse(response.responseText);
                                resolve(data);
                            } else {
                                reject(new Error(`API Error: ${response.status} - ${response.responseText}`));
                            }
                        } catch (e) {
                            reject(new Error(`Failed to parse API response: ${e.message}`));
                        }
                    },
                    onerror: (error) => reject(new Error('Network error occurred')),
                    ontimeout: () => reject(new Error('Request timed out'))
                });
            });
            
            // Get the bypassed URL from the API response
            let bypassedUrl = null;
            if (result) {
                bypassedUrl = result.bypassedUrl || result.url || result.result || result.destination || result.link;
                if (typeof result === 'string' && result.startsWith('http')) {
                    bypassedUrl = result;
                }
            }
            
            if (bypassedUrl && isValidUrl(bypassedUrl)) {
                // Redirect to the bypassed URL with redirect parameter (simulate LuArmor behavior)
                const newUrl = `${window.location.origin}${window.location.pathname}?redirect=${encodeURIComponent(bypassedUrl)}`;
                console.log('Redirecting to:', newUrl);
                window.location.replace(newUrl);
            } else {
                throw new Error('No valid bypass URL found in API response');
            }
            
        } catch (error) {
            console.error('LuArmor API bypass error:', error);
            spinner.style.display = 'none';
            btn.disabled = false;
            statusDiv.textContent = `Error: ${error.message}`;
            statusDiv.style.color = '#ff4d4d';
        }
    }

    function runLuarmorScript() {
        console.log('Running Luarmor script - exact replica of original');
        
        const urlParams = new URLSearchParams(window.location.search);
        const rawRedirect = urlParams.get('redirect');

        if (!rawRedirect) {
            // For LuArmor links without redirect parameter, use the API to get the bypassed link
            console.log('No redirect parameter found, calling bypass API for LuArmor link...');
            handleLuarmorApiBypass();
            return;
        }

            let redirectUrl = rawRedirect;

            if (!isValidUrl(redirectUrl)) {
                try {
                    const decoded = decodeURIComponent(rawRedirect);
                    if (isValidUrl(decoded)) {
                        redirectUrl = decoded;
                    } else {
                        throw new Error('Invalid redirect URL after decoding');
                    }
                } catch (err) {
                    showLuarmorError('Error: Invalid or malformed redirect URL. Please try again.');
                    return;
                }
            }

            console.log('Valid redirect URL found:', redirectUrl);

            const container = createLuarmorContainer();
            if (document.body) {
                document.body.appendChild(container);
            } else {
                document.documentElement.appendChild(container);
            }

            const countdownEl = container.querySelector('#countdown');
            const btn = container.querySelector('#nextBtn');
            const spinner = container.querySelector('#spinner');

            const newBtn = btn; // element is controlled by us; no need to clone
            const hasHash = (url) => {
                try {
                    return new URL(url).searchParams.has('hash') || url.includes('hash=');
                } catch {
                    return url.includes('hash=');
                }
            };

            if (hasHash(redirectUrl)) {
                let time = 8;
                countdownEl.style.color = '#ff4d4d';
                countdownEl.style.fontWeight = 'bold';
                const interval = setInterval(() => {
                    countdownEl.textContent = `YOU HAVE EXACTLY ${time} SECONDS TO CLICK THE BUTTON BEFORE YOUR HASH EXPIRES`;
                    time--;
                    if (time < 0) {
                        clearInterval(interval);
                        countdownEl.textContent = 'HASH EXPIRED. RETRYING...';
                        countdownEl.style.color = '';
                        countdownEl.style.fontWeight = '';
                        newBtn.disabled = true;
                        spinner.style.display = 'block';
                        setTimeout(() => {
                            location.replace(location.href.split('?')[0]);
                        }, 3500);
                    }
                }, 1000);
            } else {
                countdownEl.style.display = 'none';
            }

            const performRedirect = () => {
                if (!redirectUrl || newBtn.disabled) return;
                try {
                    newBtn.disabled = true;
                    spinner.style.display = 'block';
                    setTimeout(() => {
                        try {
                            window.location.assign(redirectUrl);
                        } catch (err) {
                            window.location.href = redirectUrl;
                        }
                    }, 60);
                } catch (err) {
                    showLuarmorError('Redirect failed. Please copy and open the link manually: ' + redirectUrl);
                    newBtn.disabled = false;
                    spinner.style.display = 'none';
                }
            };

            newBtn.addEventListener('click', (e) => {
                e.preventDefault();
                e.stopPropagation();
                performRedirect();
            }, { passive: false });

            newBtn.addEventListener('touchend', (e) => {
                e.preventDefault();
                e.stopPropagation();
                performRedirect();
            }, { passive: false });

            container.addEventListener('click', (e) => {
                if (e.target && e.target.id === 'nextBtn') return;
            });

            try {
                newBtn.setAttribute('aria-label', 'Proceed to link');
                newBtn.tabIndex = 0;
            } catch (err) { /* silent */ }
    }
    
    // Check if we're on luarmor.net or its subdomains first
    const hostname = window.location.hostname;
    const isLuarmor = hostname === 'luarmor.net' || hostname.endsWith('.luarmor.net');
    
    if (isLuarmor) {
        console.log('Luarmor domain detected:', hostname);
        console.log('Current URL:', window.location.href);
        console.log('Document ready state:', document.readyState);
        
        // Use luarmor-specific system - match original structure exactly
        try {
            if (document.readyState === 'loading') {
                document.addEventListener('DOMContentLoaded', runLuarmorScript, { once: true });
            } else {
                runLuarmorScript();
            }
        } catch (err) {
            console.error('Luarmor userscript error:', err);
            if (document.body) {
                document.body.innerHTML = `<div style="color: #ff4d4d; text-align: center; padding: 40px; background: #121212; height: 100vh; display: flex; align-items: center; justify-content: center; font-size: 1.2em;">Error in bypass script: ${err && err.message ? err.message : err}. Please reload the page.</div>`;
            }
        }
        return; // Exit early - don't run regular system
    }
    
    const API_BASE = 'https://api.bypass.vip/premium';

    // Regular API functions for non-Luarmor sites
    function createContainer() {
        const container = document.createElement('div');
        container.id = 'userscript-container';
        container.style.cssText = `
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-color: #121212;
            color: #e0e0e0;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            text-align: center;
            padding: 40px;
            z-index: 2147483647;
            font-family: 'Arial', sans-serif;
            overflow: auto;
            box-shadow: inset 0 0 20px rgba(0, 0, 0, 0.5);
            pointer-events: auto;
        `;
        container.innerHTML = `
            <h2 style="font-size: 2em; margin-bottom: 15px; color: #ffffff; text-shadow: 0 2px 4px rgba(0, 0, 0, 0.3);">BYPASS.VIP USERSCRIPT</h2>
            <p style="margin-bottom: 30px; font-size: 1.1em; color: #b0b0b0;">Click the button below to proceed to the bypassed link.</p>
            <div id="countdown" style="font-size: 1.3em; margin-bottom: 30px; padding: 10px; background: #1e1e1e; border-radius: 8px; width: 80%; max-width: 600px;"></div>
            <button id="nextBtn" type="button" style="
                padding: 12px 24px;
                background-color: #1E88E5;
                color: #ffffff;
                border: none;
                border-radius: 8px;
                cursor: pointer;
                transition: background-color 0.3s, transform 0.2s;
                font-size: 1.1em;
                box-shadow: 0 4px 8px rgba(0, 0, 0, 0.4);
                position: relative;
                z-index: 2147483647;
                pointer-events: auto;
            ">Next</button>
            <div id="errorMsg" style="color: #ff4d4d; margin-top: 30px; display: none; font-size: 1.1em; background: #2a2a2a; padding: 10px; border-radius: 8px;"></div>
            <div id="spinner" style="border: 5px solid #333333; border-top: 5px solid #1E88E5; border-radius: 50%; width: 30px; height: 30px; animation: spin 1s linear infinite; display: none; margin-top: 20px;"></div>
            <style>
                @keyframes spin { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }
                #nextBtn:hover { background-color: #1565C0; transform: translateY(-2px); }
                #nextBtn:active { transform: translateY(0); }
                #nextBtn:disabled {
                    pointer-events: none;
                    opacity: 0.6;
                    cursor: not-allowed;
                }
            </style>
        `;
        return container;
    }

    // Regular API functions for non-Luarmor sites
    async function bypassUrl(url) {
        return new Promise((resolve, reject) => {
            const requestUrl = `${API_BASE}/bypass?url=${encodeURIComponent(url)}`;
            const requestHeaders = {
                'x-api-key': config.apiKey,
                'Content-Type': 'application/json'
            };
            
            console.log('Making bypass request:');
            console.log('- URL:', requestUrl);
            console.log('- Headers:', requestHeaders);
            console.log('- Current page URL:', url);
            console.log('- API Key length:', config.apiKey ? config.apiKey.length : 'null/undefined');
            console.log('- API Key starts with:', config.apiKey ? config.apiKey.substring(0, 8) + '...' : 'null');
            
            GM_xmlhttpRequest({
                method: 'GET',
                url: requestUrl,
                headers: requestHeaders,
                onload: (response) => {
                    try {
                        console.log('Bypass API Response:', response.status, response.responseText);
                        if (response.status === 200) {
                            const data = JSON.parse(response.responseText);
                            console.log('Parsed bypass data:', data);
                            resolve(data);
                        } else {
                            reject(new Error(`API Error: ${response.status} - ${response.statusText} - ${response.responseText}`));
                        }
                    } catch (e) {
                        reject(new Error(`Failed to parse API response: ${e.message} - Raw response: ${response.responseText}`));
                    }
                },
                onerror: (error) => reject(new Error('Network error occurred')),
                ontimeout: () => reject(new Error('Request timed out'))
            });
        });
    }

    async function refreshBypass(url) {
        return new Promise((resolve, reject) => {
            const requestUrl = `${API_BASE}/refresh?url=${encodeURIComponent(url)}`;
            const requestHeaders = {
                'x-api-key': config.apiKey,
                'Content-Type': 'application/json'
            };
            
            console.log('Making refresh request:');
            console.log('- URL:', requestUrl);
            console.log('- Headers:', requestHeaders);
            console.log('- Current page URL:', url);
            console.log('- API Key length:', config.apiKey ? config.apiKey.length : 'null/undefined');
            console.log('- API Key starts with:', config.apiKey ? config.apiKey.substring(0, 8) + '...' : 'null');
            
            GM_xmlhttpRequest({
                method: 'GET',
                url: requestUrl,
                headers: requestHeaders,
                timeout: 30000, // 30 second timeout for refresh (longer than bypass)
                onload: (response) => {
                    try {
                        console.log('Refresh API Response:', response.status, response.responseText);
                        if (response.status === 200) {
                            const data = JSON.parse(response.responseText);
                            console.log('Parsed refresh data:', data);
                            resolve(data);
                        } else if (response.status === 500) {
                            // Handle 500 errors more gracefully for refresh endpoint
                            let errorMessage = 'Server error occurred';
                            try {
                                const errorData = JSON.parse(response.responseText);
                                if (errorData.message) {
                                    errorMessage = errorData.message;
                                }
                            } catch (e) {
                                // Use default message if can't parse error
                            }
                            reject(new Error(`Refresh API Error: ${errorMessage}. Try the regular Bypass instead.`));
                        } else {
                            reject(new Error(`API Error: ${response.status} - ${response.statusText} - ${response.responseText}`));
                        }
                    } catch (e) {
                        reject(new Error(`Failed to parse API response: ${e.message} - Raw response: ${response.responseText}`));
                    }
                },
                onerror: (error) => reject(new Error('Network error occurred during refresh')),
                ontimeout: () => reject(new Error('Refresh request timed out - try regular Bypass instead'))
            });
        });
    }

    // Notification popup creation
    function createNotification() {
        // Remove any existing notification
        const existing = document.getElementById('bypass-notification');
        if (existing) existing.remove();

        // Create overlay container with maximum z-index
        const overlay = document.createElement('div');
        overlay.id = 'bypass-overlay-root';
        overlay.style.cssText = `
            position: fixed !important;
            top: 0 !important;
            left: 0 !important;
            right: 0 !important;
            bottom: 0 !important;
            pointer-events: none !important;
            z-index: 2147483647 !important;
            isolation: isolate !important;
        `;

        const notification = document.createElement('div');
        notification.id = 'bypass-notification';
        notification.innerHTML = `
            <div style="
                position: absolute !important;
                top: 10px !important;
                right: 10px !important;
                left: auto !important;
                bottom: auto !important;
                background: #2d3748 !important;
                border: 2px solid #4a90e2 !important;
                border-radius: 12px !important;
                padding: 15px !important;
                width: calc(100vw - 40px) !important;
                max-width: 350px !important;
                min-width: 280px !important;
                box-shadow: 0 10px 30px rgba(0,0,0,0.8) !important;
                font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', system-ui, sans-serif !important;
                color: white !important;
                z-index: 999999 !important;
                animation: slideIn 0.3s ease-out !important;
                pointer-events: auto !important;
                will-change: transform !important;
                transform: translateZ(0) translateX(0) !important;
                backface-visibility: hidden !important;
                contain: layout style paint !important;
            ">
                <style>
                    @keyframes slideIn {
                        from { transform: translateX(100%); opacity: 0; }
                        to { transform: translateX(0); opacity: 1; }
                    }
                    @keyframes spin {
                        from { transform: rotate(0deg); }
                        to { transform: rotate(360deg); }
                    }
                    
                    /* Mobile viewport support */
                    @media screen and (max-width: 480px) {
                        #bypass-notification > div {
                            top: 5px !important;
                            right: 5px !important;
                            left: 5px !important;
                            width: calc(100vw - 20px) !important;
                            max-width: none !important;
                            padding: 12px !important;
                        }
                        .notification-btn {
                            padding: 10px 12px !important;
                            margin: 2px !important;
                            font-size: 14px !important;
                            min-height: 44px !important;
                            touch-action: manipulation !important;
                        }
                        #notif-close-btn {
                            min-width: 44px !important;
                            min-height: 44px !important;
                            font-size: 16px !important;
                        }
                    }
                    
                    .notification-btn {
                        background: #4a90e2 !important;
                        border: none !important;
                        color: white !important;
                        padding: 10px 16px !important;
                        margin: 4px !important;
                        border-radius: 6px !important;
                        cursor: pointer !important;
                        font-size: 13px !important;
                        font-weight: 500 !important;
                        transition: all 0.2s !important;
                        pointer-events: auto !important;
                        position: relative !important;
                        z-index: 999999 !important;
                        min-height: 40px !important;
                        touch-action: manipulation !important;
                        -webkit-tap-highlight-color: transparent !important;
                    }
                    .notification-btn:hover {
                        background: #357abd;
                        transform: translateY(-1px);
                    }
                    .notification-btn:disabled {
                        opacity: 0.6;
                        cursor: not-allowed;
                        transform: none;
                    }
                    .btn-secondary { background: #e53e3e; }
                    .btn-secondary:hover { background: #c53030; }
                    .btn-settings { background: #38a169; }
                    .btn-settings:hover { background: #2f855a; }
                    .spinner {
                        display: inline-block;
                        width: 12px;
                        height: 12px;
                        border: 2px solid #333;
                        border-top: 2px solid #4a90e2;
                        border-radius: 50%;
                        animation: spin 1s linear infinite;
                        margin-right: 6px;
                    }
                </style>
                
                <div style="display: flex; align-items: center; margin-bottom: 15px;">
                    <div style="
                        width: 40px;
                        height: 40px;
                        background: #4a90e2;
                        border-radius: 50%;
                        display: flex;
                        align-items: center;
                        justify-content: center;
                        margin-right: 12px;
                        font-size: 18px;
                    ">🔓</div>
                    <div>
                        <h3 style="margin: 0; font-size: 16px; font-weight: 600;">Bypass Available</h3>
                        <p style="margin: 0; font-size: 13px; color: #a0aec0;">Premium bypass ready</p>
                    </div>
                </div>

                <div id="notification-status" style="
                    background: rgba(255,255,255,0.1);
                    padding: 8px 12px;
                    border-radius: 6px;
                    font-size: 13px;
                    margin-bottom: 15px;
                    text-align: center;
                ">Ready to bypass</div>

                <div style="
                    background: rgba(74, 144, 226, 0.1);
                    border: 1px solid rgba(74, 144, 226, 0.3);
                    padding: 8px 12px;
                    border-radius: 6px;
                    font-size: 11px;
                    margin-bottom: 15px;
                    text-align: center;
                    color: #a0aec0;
                ">
                    💡 Use "Refresh" if the link has recently been updated
                </div>

                <div id="countdown-display" style="
                    display: none;
                    background: rgba(255, 193, 7, 0.15);
                    border: 1px solid rgba(255, 193, 7, 0.4);
                    padding: 10px;
                    border-radius: 6px;
                    margin-bottom: 15px;
                    text-align: center;
                ">
                    <div style="font-size: 24px; font-weight: bold; color: #ffc107; margin-bottom: 5px;" id="countdown-timer">5</div>
                    <div style="font-size: 12px; color: #a0aec0; margin-bottom: 8px;">Auto-redirect in progress...</div>
                    <button id="stop-countdown-btn" class="notification-btn" style="background: #e53e3e; font-size: 12px; padding: 6px 12px;">
                        ⏹ Stop Auto-Redirect
                    </button>
                </div>

                <div style="display: flex; flex-wrap: wrap; gap: 6px; justify-content: center;">
                    <button id="notif-redirect-btn" class="notification-btn">
                        <span class="btn-text">Bypass</span>
                    </button>
                    <button id="notif-refresh-btn" class="notification-btn btn-secondary">
                        <span class="btn-text">Refresh</span>
                    </button>
                    <button id="notif-close-btn" class="notification-btn" style="background: #666; font-size: 14px; min-width: 32px; min-height: 32px; display: flex; align-items: center; justify-content: center; cursor: pointer; user-select: none; -webkit-user-select: none;">
                        ✕
                    </button>
                </div>
            </div>
        `;

        // Append notification to overlay
        overlay.appendChild(notification);
        return overlay;
    }

    function showModal() {
        if (document.getElementById('bypass-overlay-root')) return;

        const overlayWithNotification = createNotification();

        const target = document.body || document.documentElement;
        if (!target) {
            console.error('Unable to insert notification overlay: no document body present');
            return;
        }

        target.appendChild(overlayWithNotification);
        console.log('Bypass overlay inserted');

        const statusDiv = document.getElementById('notification-status');
        const redirectBtn = document.getElementById('notif-redirect-btn');
        const refreshBtn = document.getElementById('notif-refresh-btn');
        const closeBtn = document.getElementById('notif-close-btn');
        const countdownDisplay = document.getElementById('countdown-display');
        const countdownTimer = document.getElementById('countdown-timer');
        const stopCountdownBtn = document.getElementById('stop-countdown-btn');

        let countdownInterval = null;
        let autoRedirectTimeout = null;

        function startCountdown(seconds) {
            // Clear any existing countdown
            if (countdownInterval) clearInterval(countdownInterval);
            if (autoRedirectTimeout) clearTimeout(autoRedirectTimeout);
            
            // Show countdown display
            countdownDisplay.style.display = 'block';
            statusDiv.textContent = `Auto-redirect in ${seconds} seconds...`;
            statusDiv.style.background = 'rgba(255, 193, 7, 0.2)';
            
            let countdown = seconds;
            countdownTimer.textContent = countdown;
            
            countdownInterval = setInterval(() => {
                countdown--;
                countdownTimer.textContent = countdown;
                statusDiv.textContent = `Auto-redirect in ${countdown} seconds...`;
                
                if (countdown <= 0) {
                    clearInterval(countdownInterval);
                    countdownTimer.textContent = '0';
                    statusDiv.textContent = 'Auto-redirecting...';
                    statusDiv.style.background = 'rgba(255, 193, 7, 0.3)';
                    
                    // Trigger the bypass
                    handleRedirect(false);
                }
            }, 1000);
        }

        function stopCountdown() {
            if (countdownInterval) {
                clearInterval(countdownInterval);
                countdownInterval = null;
            }
            if (autoRedirectTimeout) {
                clearTimeout(autoRedirectTimeout);
                autoRedirectTimeout = null;
            }
            
            countdownDisplay.style.display = 'none';
            statusDiv.textContent = 'Auto-redirect stopped';
            statusDiv.style.background = 'rgba(255,255,255,0.1)';
            
            // Reset status after 2 seconds
            setTimeout(() => {
                statusDiv.textContent = 'Ready to bypass';
            }, 2000);
        }

        // Show countdown if auto-redirect is enabled from settings
        if (config.autoRedirect2s || config.autoRedirect5s) {
            const delay = config.autoRedirect2s ? 2 : 5;
            startCountdown(delay);
        }

        // Helper function to validate if response is a proper URL
        function isValidUrl(string) {
            try {
                const url = new URL(string);
                return url.protocol === 'http:' || url.protocol === 'https:';
            } catch (_) {
                return false;
            }
        }

        // Function to display non-URL text responses
        function displayTextResponse(text) {
            // Remove loading spinner and update status
            redirectBtn.disabled = false;
            refreshBtn.disabled = false;
            redirectBtn.querySelector('.btn-text').textContent = 'Bypass';
            refreshBtn.querySelector('.btn-text').textContent = 'Refresh';
            
            statusDiv.innerHTML = `
                <div style="color: #fbbf24; margin-bottom: 8px;">⚠️ Response is not a URL - displaying content:</div>
                <div style="background: rgba(0,0,0,0.3); border: 1px solid #4a5568; border-radius: 8px; padding: 12px; max-height: 150px; overflow-y: auto; font-family: 'Courier New', monospace; font-size: 12px; line-height: 1.4; word-wrap: break-word; white-space: pre-wrap;">${text}</div>
                <div style="margin-top: 8px; display: flex; gap: 8px;">
                    <button onclick="navigator.clipboard.writeText('${text.replace(/'/g, "\\'")}').then(() => this.textContent = 'Copied!').catch(() => this.textContent = 'Copy failed')" 
                            style="background: #4299e1; color: white; border: none; padding: 6px 12px; border-radius: 4px; font-size: 11px; cursor: pointer;">
                        📋 Copy Text
                    </button>
                    <button onclick="this.parentElement.parentElement.parentElement.querySelector('#notif-close-btn').click()" 
                            style="background: #666; color: white; border: none; padding: 6px 12px; border-radius: 4px; font-size: 11px; cursor: pointer;">
                        Close
                    </button>
                </div>
            `;
            statusDiv.style.background = 'rgba(251, 191, 36, 0.2)';
        }

        async function handleRedirect(useRefresh = false) {
            const currentUrl = window.location.href;
            
            // Stop any ongoing countdown when manually triggering redirect
            if (countdownInterval) {
                clearInterval(countdownInterval);
                countdownInterval = null;
            }
            if (autoRedirectTimeout) {
                clearTimeout(autoRedirectTimeout);
                autoRedirectTimeout = null;
            }
            countdownDisplay.style.display = 'none';
            
            try {
                // Disable buttons and show loading
                redirectBtn.disabled = true;
                refreshBtn.disabled = true;
                const targetBtn = useRefresh ? refreshBtn : redirectBtn;
                const btnText = targetBtn.querySelector('.btn-text');
                const originalText = btnText.textContent;
                btnText.innerHTML = '<div class="spinner"></div>Processing...';
                
                statusDiv.textContent = useRefresh ? 'Using refresh endpoint...' : 'Bypassing link...';

                let result;
                if (useRefresh) {
                    result = await refreshBypass(currentUrl);
                } else {
                    result = await bypassUrl(currentUrl);
                }

                // Check for various possible response formats
                let responseContent = null;
                if (result) {
                    // Try different possible property names
                    responseContent = result.bypassedUrl || result.url || result.result || result.destination || result.link;
                    
                    // If it's a string response, use it directly
                    if (typeof result === 'string') {
                        responseContent = result;
                    }
                }

                if (responseContent) {
                    // Validate if the response is a proper URL
                    if (isValidUrl(responseContent)) {
                        // Valid URL - proceed with redirect
                        statusDiv.textContent = 'Success! Redirecting...';
                        
                        setTimeout(() => {
                            if (config.newTab) {
                                window.open(responseContent, '_blank');
                                // Keep notification open when using new tab
                                statusDiv.textContent = '✅ Link opened in new tab';
                                statusDiv.style.background = 'rgba(56, 161, 105, 0.2)';
                                
                                // Re-enable buttons for multiple uses
                                redirectBtn.disabled = false;
                                refreshBtn.disabled = false;
                                redirectBtn.querySelector('.btn-text').textContent = 'Bypass';
                                refreshBtn.querySelector('.btn-text').textContent = 'Refresh';
                            } else {
                                window.location.href = responseContent;
                                // Don't close modal for same-tab redirect since page is changing
                            }
                        }, 1000);
                    } else {
                        // Not a valid URL - display as text content
                        console.log('Response is not a valid URL, displaying as text:', responseContent);
                        displayTextResponse(responseContent);
                    }
                } else {
                    throw new Error(`No response content found. API returned: ${JSON.stringify(result)}`);
                }

            } catch (error) {
                console.error('Bypass error:', error);
                statusDiv.innerHTML = `<span style="color: #ff4444;">Error: ${error.message}</span>`;
                statusDiv.style.background = 'rgba(229, 62, 62, 0.2)';
                
                // Re-enable buttons and reset their content
                setTimeout(() => {
                    redirectBtn.disabled = false;
                    refreshBtn.disabled = false;
                    
                    const redirectBtnText = redirectBtn.querySelector('.btn-text');
                    const refreshBtnText = refreshBtn.querySelector('.btn-text');
                    
                    if (redirectBtnText) redirectBtnText.textContent = 'Bypass';
                    if (refreshBtnText) refreshBtnText.textContent = 'Refresh';
                    
                    // Reset status after 5 seconds
                    setTimeout(() => {
                        statusDiv.textContent = 'Ready to bypass';
                        statusDiv.style.background = 'rgba(255,255,255,0.1)';
                    }, 5000);
                }, 100); // Small delay to ensure DOM is ready
            }
        }

        function showSettings() {
            // Remove existing settings if any
            const existing = document.getElementById('bypass-settings-notification');
            if (existing) existing.remove();

            const settingsModal = createSettingsModal();
            document.body.appendChild(settingsModal);

            // Handle mutual exclusivity of auto-redirect options
            const autoRedirect2sCheckbox = document.getElementById('settings-auto-redirect-2s');
            const autoRedirect5sCheckbox = document.getElementById('settings-auto-redirect-5s');
            
            autoRedirect2sCheckbox.addEventListener('change', () => {
                if (autoRedirect2sCheckbox.checked) {
                    autoRedirect5sCheckbox.checked = false;
                }
            });
            
            autoRedirect5sCheckbox.addEventListener('change', () => {
                if (autoRedirect5sCheckbox.checked) {
                    autoRedirect2sCheckbox.checked = false;
                }
            });

            document.getElementById('settings-save-btn').onclick = () => {
                config.apiKey = document.getElementById('settings-api-key').value.trim();
                config.newTab = document.getElementById('settings-new-tab').checked;
                
                // Handle auto-redirect options - ensure only one can be active
                const autoRedirect2s = document.getElementById('settings-auto-redirect-2s').checked;
                const autoRedirect5s = document.getElementById('settings-auto-redirect-5s').checked;
                
                if (autoRedirect2s && autoRedirect5s) {
                    // If both are checked, keep only the 2s option
                    config.autoRedirect2s = true;
                    config.autoRedirect5s = false;
                } else {
                    config.autoRedirect2s = autoRedirect2s;
                    config.autoRedirect5s = autoRedirect5s;
                }
                
                saveConfig(config);
                
                document.getElementById('bypass-settings-notification').remove();
                
                // Show success message briefly
                statusDiv.textContent = '✅ Settings saved!';
                statusDiv.style.background = 'rgba(56, 161, 105, 0.2)';
                setTimeout(() => {
                    statusDiv.textContent = 'Ready to bypass';
                    statusDiv.style.background = 'rgba(255,255,255,0.1)';
                }, 2000);
            };

            document.getElementById('settings-cancel-btn').onclick = () => {
                document.getElementById('bypass-settings-notification').remove();
            };
        }

        function closeModal() {
            // Clean up any active countdown timers
            if (countdownInterval) {
                clearInterval(countdownInterval);
                countdownInterval = null;
            }
            if (autoRedirectTimeout) {
                clearTimeout(autoRedirectTimeout);
                autoRedirectTimeout = null;
            }
            
            // Remove the entire overlay root to ensure complete cleanup
            const overlay = document.getElementById('bypass-overlay-root');
            if (overlay) {
                overlay.remove();
                console.log('Notification closed successfully');
            }
            
            // Fallback: also try to remove notification directly
            const notification = document.getElementById('bypass-notification');
            if (notification) {
                notification.remove();
            }
        }

        // Event listeners
        redirectBtn.onclick = () => {
            if (countdownInterval) clearInterval(countdownInterval);
            handleRedirect(false);
        };
        refreshBtn.onclick = () => {
            if (countdownInterval) clearInterval(countdownInterval);
            
            // Show message that refresh endpoint is down
            refreshBtn.disabled = true;
            const btnText = refreshBtn.querySelector('.btn-text');
            if (btnText) {
                btnText.innerHTML = '⚠️ Endpoint Down';
            }
            
            statusDiv.innerHTML = `
                <div style="color: #fbbf24; margin-bottom: 8px; text-align: center;">
                    ⚠️ <strong>Refresh/Update Endpoint Currently Down</strong>
                </div>
                <div style="font-size: 12px; line-height: 1.4; text-align: center; color: #e2e8f0;">
                    The Refresh/Update endpoint for Bypass.VIP is currently down. The script will be updated when the endpoint is fixed; be sure to check for updates.
                </div>
                <div style="margin-top: 12px; text-align: center;">
                    <button onclick="this.parentElement.parentElement.querySelector('#notif-close-btn').click()" 
                            style="background: #4299e1; color: white; border: none; padding: 8px 16px; border-radius: 4px; font-size: 12px; cursor: pointer;">
                        Close Notification
                    </button>
                </div>
            `;
            statusDiv.style.background = 'rgba(251, 191, 36, 0.2)';
            
            // Re-enable button after 3 seconds
            setTimeout(() => {
                refreshBtn.disabled = false;
                if (btnText) {
                    btnText.textContent = 'Refresh';
                }
            }, 3000);
        };
        if (stopCountdownBtn) {
            stopCountdownBtn.onclick = stopCountdown;
        }
        // Improved close button handler with multiple event types
        function attachCloseHandler(element) {
            if (!element) return;
            
            const closeHandler = (e) => {
                e.preventDefault();
                e.stopPropagation();
                if (countdownInterval) clearInterval(countdownInterval);
                closeModal();
            };
            
            // Attach multiple event types for better compatibility
            element.onclick = closeHandler;
            element.addEventListener('click', closeHandler, true);
            element.addEventListener('touchstart', closeHandler, true);
        }
        
        attachCloseHandler(closeBtn);

        // Close on Escape key
        document.addEventListener('keydown', (e) => {
            if (e.key === 'Escape') closeModal();
        }, { once: true });

        // Notification stays on screen until user interacts with it
    }

    // Auto redirect without modal - DISABLED because it doesn't work well for most sites
    // Most bypass sites need manual interaction to work properly
    /*
    async function autoRedirect() {
        const currentUrl = window.location.href;
        
        try {
            const result = await bypassUrl(currentUrl);
            
            // Check for various possible response formats
            let bypassedUrl = null;
            if (result) {
                bypassedUrl = result.bypassedUrl || result.url || result.result || result.destination || result.link;
                
                if (typeof result === 'string' && result.startsWith('http')) {
                    bypassedUrl = result;
                }
            }
            
            if (bypassedUrl) {
                if (config.newTab) {
                    window.open(bypassedUrl, '_blank');
                } else {
                    window.location.href = bypassedUrl;
                }
            } else {
                throw new Error('No bypass URL found in auto-redirect');
            }
        } catch (error) {
            console.error('Auto redirect failed:', error);
            // Fallback to showing modal on error
            showModal();
        }
    }
    */

    // Visibility detection function
    function isNotificationVisible() {
        const notification = document.getElementById('bypass-notification');
        if (!notification) return false;
        
        const rect = notification.getBoundingClientRect();
        const style = window.getComputedStyle(notification);
        
        // Check if notification is actually visible and clickable
        return (
            rect.width > 0 && 
            rect.height > 0 && 
            style.display !== 'none' && 
            style.visibility !== 'hidden' && 
            style.opacity !== '0' &&
            rect.top >= 0 && 
            rect.left >= 0
        );
    }



    // Initialize the script with full page load wait and visibility detection (NON-LUARMOR SITES ONLY)
    function init() {
        console.log('Bypass.VIP Premium Bypasser v3.2.1 - Initializing regular bypass system...');
        
        // Wait for complete page load including all resources
        function waitForFullPageLoad() {
            return new Promise((resolve) => {
                if (document.readyState === 'complete') {
                    // Page already fully loaded
                    resolve();
                } else {
                    // Wait for full load event
                    window.addEventListener('load', resolve, { once: true });
                }
            });
        }
        
        // Additional wait for dynamic content
        function waitForPageStabilization() {
            return new Promise((resolve) => {
                // Wait extra time for dynamic content and overlays to load
                setTimeout(resolve, 2000);
            });
        }
        
        async function start() {
            try {
                // Step 1: Wait for full page load
                await waitForFullPageLoad();
                console.log('Page fully loaded, waiting for stabilization...');
                
                // Step 2: Wait for page content to stabilize
                await waitForPageStabilization();
                console.log('Page stabilized, showing notification...');
                
                // Step 3: Show the notification
                showModal();
                
                // Step 4: Handle auto-redirect if enabled
                if (config.autoRedirect2s || config.autoRedirect5s) {
                    const autoDelay = config.autoRedirect2s ? 2000 : 5000;
                    
                    setTimeout(async () => {
                        const redirectBtn = document.getElementById('notif-redirect-btn');
                        if (redirectBtn && !redirectBtn.disabled && isNotificationVisible()) {
                            console.log('Auto-redirect triggered');
                            redirectBtn.click();
                        }
                    }, autoDelay);
                }
                
            } catch (error) {
                console.error('Bypass.VIP initialization error:', error);
                // Fallback - try to show notification anyway
                setTimeout(showModal, 1000);
            }
        }
        
        // Start the initialization
        start();
    }

    // Start the script
    init();
})();